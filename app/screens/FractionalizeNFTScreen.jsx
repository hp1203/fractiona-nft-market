import { Text, View } from "react-native";
import Layout from "../components/Layout";

const FractionalizeNFTScreen = () => {
  return (
    <Layout>
      <Text>Fractionalize NFT</Text>
    </Layout>
  );
};

export default FractionalizeNFTScreen;
